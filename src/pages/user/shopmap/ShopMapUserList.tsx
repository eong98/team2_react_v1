
import './ShopMapUserList.css';

// SHOPMAP API 데이터
interface ShopMap {
    sno: number;
    no: number;
    fname: string;
    fsaved: string;
    cdate: string;
}

// 매장 정보에 도면 등록 정보를 추가


export default function ShopMapUserList() {

    return (
        <div>0</div>
    );
}